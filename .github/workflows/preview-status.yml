name: Preview Status

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created, edited]

permissions:
  statuses: write

jobs:
  set-pending:
    name: Dokploy Preview
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Set pending commit status
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.pull_request.head.sha,
              state: "pending",
              context: "Dokploy Preview",
              description: "Waiting for preview deployment...",
            });

  check-comment:
    name: Dokploy Preview Gate
    if: |
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      contains(github.event.comment.user.login, 'dokploy')
    runs-on: ubuntu-latest
    steps:
      - name: Parse Dokploy comment and update status
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.comment.body || "";

            if (!body.includes("Preview Deployment")) return;

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.issue.number,
            });

            let state = "pending";
            let description = "Preview deployment in progress...";

            if (body.includes("✅")) {
              state = "success";
              description = "Preview deployment ready";
            } else if (body.includes("❌")) {
              state = "failure";
              description = "Preview deployment failed";
            }

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: pr.head.sha,
              state,
              context: "Dokploy Preview",
              description,
            });

            core.info(`Set commit status to ${state} for ${pr.head.sha.slice(0, 7)}`);
